# Comistream - Handle special characters in URLs
# Fix for AH10411: Rewritten query string contains control characters or spaces

# AllowEncodedSlashes must be at server or virtual host level, not in Directory context
AllowEncodedSlashes NoDecode
